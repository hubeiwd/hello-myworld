# hello-myworld
a new origin
int FindSet(S[],len,x)
    {
       for  (i=1;i<=len;i++)
         if (S[i]==x])
            break;
            if (i<=len)
            {
            x=S[i];
            return(TRUE);
            }
            else
            return(FALSE);
    }
